# Internal Press feature-bench TcUnit evidence — 2026-08-06

Gate: `FraktalCore/PLC/TwinCAT/Examples/PressDemo/PressTests.plcproj`

Scope: framework integration testing only. Press Demo is an internal simulated
test bench, not a real machine project or production acceptance target.

Runtime: isolated VM identified by the operator as `192.168.1.5`, AMS Net ID
`192.168.132.128.1.1`, PLC ADS port 851, Autostart Boot Project disabled. The
event stream identifies the *initiating engineering* AMS Net ID as
`192.168.1.6.1.1` — this development PC, not the target. §6.1 is explicit that
the target must never be inferred from that line, and here the distinction
matters: an earlier attempt on this workstation activated against the LOCAL
usermode runtime precisely because the solution carried no `TargetNetId` and
`192.168.1.6.1.1` is what it defaulted to. That attempt produced no `PlcTask`
output at all. `TargetNetId` is now recorded in the solution.

Repository revision: `2f3f1c72edccc2b6332ba1b242cb8a462a2183b7`
(`ladder-en-form-and-hold-rollup`), working tree otherwise clean.

| Field | Result |
|---|---:|
| Runner | `PRG_PressTestRunner` |
| Successful tests | 8 |
| Failed tests | 0 |
| Total tests | 8 |
| Test suites | 2 |
| Duration | 0.0098288 s |

Both intended suites ran — `PressDemoTests` and `FaultRecoveryTests`, four tests
each. `tools/tcunit_to_junit.py` accepted the raw output against expected runner
`PRG_PressTestRunner`, 8 tests and 2 suites, and emitted a passing JUnit gate
(`2026-08-06_Press_TcUnit.junit.xml`).

## What this run does and does not cover

This revision completed the LD rendition of the AUTO chain (16 rungs) and added
an LD rendition of the release/interlock logic. **Neither was exercised here.**
`FB_PressDemoUnit.AUTO_SEQUENCE_LANGUAGE` and `RELEASE_LANGUAGE` are both `0` in
the committed source, so the Unit bound and ran the **ST** renditions. The two
ladder bodies are compiled, graph-verified against their object model, and
cross-checked step-for-step against the ST twin — but they have not executed on
a runtime. Re-running this same gate with either constant set to `2` is what
would close that, and it is not claimed here.

Non-blocking messages in the log, consistent with previous runs on this bench:
`TwinCAT USB support could not be initialized` (the log itself says system start
continued, and a simulated gate needs no USB hardware) and `SEH for TMX files in
WinNT kernel threads is disabled`.

## Core/Modules gate

Not run at this revision. `Fraktal_Tests` had been carried as a known red — six
visible compile errors hiding seven more — so its runtime gate could not run at
all, because a project that does not compile cannot be downloaded. It compiles
as of this revision; the expected inventory is **94 tests across 29 suites**,
not the 84/26 of the 2026-08-02 baseline (`FB_SequenceRaise_Tests`,
`FB_SequencePar_Tests` and `FB_StateFlag_Tests` were added since).

## SHA-256 snapshot

| Artifact | SHA-256 |
|---|---|
| Archived raw XAE/TcUnit output | `98be6daea0e11341f3638ad508660e5a260f779572807eadf0226541b3b7203c` |
| `PressTests.plcproj` | `392f269c9978beaa27e80758603eaa9a195de1ccb54ccf0d3ed6ab93cfdfc85a` |
| `PressTests/PRG_PressTestRunner.TcPOU` | `24468d01887538547794dc71eaad00b223282fe1887dafe03900af21d977d3fd` |
| `PressTests/PlcTask.TcTTO` | `e64078b3f0f7c36c1ae765ebbf2754068ae1edb0c0899c940124007753a1236d` |
| Generated `PressTests.tmc` | `0cd775ce4ae7b69bdae0416e5370677dde5921d87528db55c5500455a04e60e7` |

The `.tmc` is regenerated by activation and its hash therefore binds this run,
not the source; the runner and task hashes are unchanged from 2026-08-02, which
is the check that the same gate ran.
