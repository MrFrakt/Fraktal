# PLC ignores are centralized in the repo-root .gitignore (O9: single source):
# TwinCAT per-machine build output (_CompileInfo/, _Boot/, *.compileinfo) is
# ignored there. Everything authored is TRACKED — including scaffold/, the
# copy-template for a new module type (ships SKELETON.md; §2 quick-start).
